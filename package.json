{
  "name": "feedback-plugin-highway-3d",
  "version": "1.0.0",
  "private": true,
  "description": "3D Highway visualization plugin for FeedBack — standalone fork, being split from a single screen.js into an ES-module graph under src/.",
  "license": "AGPL-3.0-only",
  "scripts": {
    "test:js": "node --test tests/*.test.js tests/*.test.mjs tests/legacy/*.test.js tests/legacy/*.test.mjs",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.6",
    "eslint": "^9.39.4",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.32.0",
    "globals": "^17.9.0"
  }
}
