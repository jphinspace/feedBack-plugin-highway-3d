# eslint-config-airbnb-base@15's peer dep (eslint ^7||^8) predates ESLint 9;
# the rule set itself works fine on 9 (see eslint.config.js's header comment).
# Without this, plain `npm install`/`npm ci` fail with ERESOLVE.
legacy-peer-deps=true
